import random

class randomisation_functions:
    def exemple(data_dump,seed,ignored_stages_files):
        random.seed(seed)
        input(data_dump) # DEBUG : List Data Dump for helping to create randomizer
        # Write randomization script here
        
        return data_dump